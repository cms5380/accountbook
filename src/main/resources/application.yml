spring:
  datasource:
    url: jdbc:mysql://localhost:3306/myDB?useSSL=false&useUnicode=true&characterEncoding=utf8
    username: root
    password: 1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  redis:
    host: localhost
    port: 6379

jwt:
  secret: charminseok
  access-expired: 1800

logging:
  level:
    root: info